---
name: Bug report
about: Something is broken
labels: bug
---

**What happened**

**Steps to reproduce**

**Expected behavior**

**Environment**

- OS:
- Python version (`python --version`):
- crawl4ai version (`uv run python -c "import crawl4ai; print(crawl4ai.__version__)"`)
- MCP client and version:

**Relevant logs**
Run `uv run python -m crawl4ai_mcp.server 2>&1 1>/dev/null` and paste stderr output.
