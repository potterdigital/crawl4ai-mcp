## What this PR does

## Why

## Testing

- [ ] `uv run pytest` passes
- [ ] `uv run ruff check src/` passes
- [ ] No `print()` calls added (would corrupt MCP stdio transport)

## Related issues

Closes #
